info:
  name: LLM_findAllHouses
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/v1/chat/houses
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "user_prompt": "Find all the houses. Use only tool response, don't suggest from your own side"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
