info:
  name: list_models
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/v1/chat/list-models
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
