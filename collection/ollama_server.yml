info:
  name: ollama_server
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:11434/api/generate
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
       "model": "llama3.2",
       "prompt": "Why sun is hot.",
       "stream": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
