info:
  name: LLM_findHouseByPropertyId
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/v1/chat/houses
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "user_prompt": "Find a house by its property Id as 'NY009839RA7C'. Use only tool response, don't suggest from your own side"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
